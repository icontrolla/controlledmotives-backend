
# Django and related packages
Django>=5.2,<6.0
gunicorn>=20.1,<21.0  # WSGI server for deployment
whitenoise>=6.0,<7.0  # For serving static files in production

# Database dependencies (use the one relevant to your database)
psycopg2>=2.9,<3.0  # PostgreSQL driver (for PostgreSQL)
# mysqlclient>=2.1,<3.0  # Uncomment for MySQL
# pymysql>=1.0,<2.0  # Uncomment for MySQL as a pure Python alternative

# Static files
django-storages>=1.12,<2.0  # If you're using cloud storage like AWS S3 for static files

# Security
django-extensions>=3.2,<4.0  # Helpful for admin extensions, shell, etc.

# Optional: If using Celery for background tasks
celery>=5.2,<6.0

# Optional: If using Redis for caching or task queues with Celery
redis>=4.0,<5.0

# Optional: If you're using Django REST Framework
djangorestframework>=3.14,<4.0

# Optional: If using Pillow for image handling
Pillow>=9.0,<10.0

# Optional: For security improvements
django-cors-headers>=3.10,<4.0  # For handling CORS in API apps
django-crispy-forms>=1.14,<2.0  # For better form rendering

# Optional: For environment variables management
python-decouple>=3.5,<4.0  # For managing settings in production

# Optional: If you're working with logging
django-silk>=5.0,<6.0  # For profiling and performance monitoring (optional)
